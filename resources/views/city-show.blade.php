<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <title>{{$data->name_city}}</title> -->
    <title>Тест</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="{{URL::asset('css\main.css')}}">
</head>
<body>
<!-- <h1> {{$data->name_city}}</h1> -->
    <!-- <div class="alert alert-info">
        
        <p>{{$data->opis_city}}</p>
        <p>{{$data->created_at}}</p>

        <table class="table table-bordered table-hover table-striped">
    <thead>
    <tr><th>#</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        @foreach($data->img_city as $image)
        <img src="{{ asset('/image/'.$image) }}" style="height:120px; width:200px"/>
        @endforeach
    </tbody>
   </table>

           </br>
           </br> -->
        <!-- <a href="{{route('contact-update', $data->id)}}"><button class="btn btn-primary">Редактировать</button></a> </br> </br>
        <a href="{{route('contact-delete', $data->id)}}"><button class="btn btn-danger">Удалить</button></a> -->
    <!-- </div> -->
    <h1>Test</h1>
</body>
</html>